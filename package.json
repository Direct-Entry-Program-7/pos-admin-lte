{
  "name": "pos-admin-lte",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf ./docs",
    "build": "parcel build index.html 404.html --out-dir ./docs --public-url /pos-admin-lte/",
    "dev": "parcel serve manage-customers.html",
    "start": "npm run build && npm run dev"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "admin-lte": "^3.1.0"
  }
}
